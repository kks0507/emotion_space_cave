# 인수인계서 — 마음동굴 (가제)

> **이 문서의 역할**: 프로젝트의 단일 진실 공급원(Single Source of Truth).
> 모든 작업은 이 문서를 먼저 갱신하고 시작한다. "지금까지 뭘 했고 / 뭘 미뤘고 / 다음에 뭘 할지"를 여기서만 관리한다.
> 작업 세션을 시작할 때 § 5(다음 작업)와 § 4(미뤄둔 것)를 먼저 읽는다.

- **최초 작성**: 2026-06-17
- **최종 갱신**: 2026-06-17 (C 매칭방향·루브릭 확정, 데이터 라벨 반입, DB 적재 착수)
- **현재 단계**: 기획 확정 → **데이터 적재 진행 중** (Turso)

---

## 1. 한 줄 소개

문학작품 낭송 데이터(AI Hub)를 활용해, 이용자가 털어놓은 심정을 감정 분석한 뒤
그에 맞는 문학 문구 1개를 골라 **디지털 카드 + 2문장 편지**로 돌려주는 서비스.
컨셉은 **"대나무 숲 / 동굴"** — 위로뿐 아니라 축하·격려·자랑 들어주기까지 포괄한다.

### 숨은 목적 (서비스의 진짜 존재 이유)
표면은 "위로 카드 서비스"이지만, 진짜 목적은 **학생 정서를 추적·관리해 자퇴·휴학 등 학업 중단을 예방하고
학생이 학적을 끝까지 유지하도록 돕는 것**이다.
→ 따라서 "위기"의 정의는 자살 위험 같은 임상적 위기 한 종류가 아니라,
  **정서적 이탈 → 학업 중단(드롭아웃) 위험**까지 포함한다.
→ 루브릭(§ D9 점수 → 행동 연결)은 이 학업중단 조기경보(early-warning) 관점에서 설계한다.
→ 이연된 관리자 모니터링(F3)의 궁극적 용도도 위기 학생 발견을 통한 **학적 유지 개입**이다.

---

## 2. 확정된 결정 (Decisions)

| # | 항목 | 결정 | 비고 |
|---|------|------|------|
| D1 | 결과물 성숙도 | **프로덕션 지향** | MVP 검증용 아님 |
| D2 | 플랫폼 | **Next.js + Vercel 배포** | |
| D3 | DB | **Turso (libSQL)** | 확정 |
| D4 | 인증/로그인 | **붙이지 않음** | → § 4로 이연 |
| D5 | 이용자 입력 | **이름 + 연령**만 수집 | 연령은 AI 편지 톤·어휘 조절에 사용 |
| D6 | 이용 대상 | **초등생 / 대학생 2계층** | DB는 공유, 프런트(UX·라이팅)는 분리된 별도 서비스 |
| D7 | 데이터 소스 | AI Hub **문학작품 낭송·낭독 음성 데이터** | 시·소설·희곡/시나리오, 60개 감정 라벨, 문장 단위 정제. **텍스트만 사용**(음성 미사용) |
| D8 | 핵심 파이프라인 | 5단계 (아래) | |
| D9 | 2단계 감정 분석 엔진 | **하이브리드** | 부정어/위기어 사전 1차 점수(재현성·설명가능) → LLM 맥락 보정(반어·비유). 점수는 관리자 알림 판단의 근거가 되므로 설명 가능한 뼈대가 필수 |
| D10 | 데이터 확보 상태 | **미확보 — 신청부터 시작** | 전략서에 AI Hub 신청·승인·반입·전처리 단계를 포함. ERD는 추정 스키마 기반으로 설계하되, 반입 후 실제 라벨링 포맷 확인 시 보정 |
| D11 | 4단계 RAG 검색 인프라 | **Turso 네이티브 벡터 검색**(libSQL vector) | 문학 문장을 미리 임베딩해 Turso에 저장. 3단계 라벨 필터 → 4단계 벡터 유사도 정렬을 한 DB 안에서 처리. 별도 벡터 스토어 없음 |
| D12 | 출력 분기 구조 | **두 축(2-axis)** | 축①=부정도(위기 강도, 돌봄/개입 필요성) → **안전 게이트**. 축②=발화 의도/정서(7 emotion + 53 style 라벨 활용) → **결 선택**. 부정도가 게이트 선을 넘으면 무조건 돌봄 모드, 그 아래에서만 의도 축으로 위로/축하/들어주기 결정. 대표님 6번 요구(부정/비부정 출력 절대 분리)의 구조적 충족 |
| D13 | 매칭 방향 (사용자 정서 → 줄 문장) | **C: 레벨에 따라 분기** | 평상~경미(L0~L1)=공감/메아리(같은 결의 문학 문장), 경계~위기(L3~L4)=전환+위기상담 안내. 루브릭 레벨이 매칭 방향까지 결정. "동굴의 메아리" 정체성 + 위기 시 안전 |
| D14 | 단계적 루브릭 (O2 확정) | **5단계 L0~L4 (MTSS 차용)** | 위기 강도 = max(축A 부정정서강도 0~4, 축B 이탈의사구체성 0~4). L0 일상/L1 경미(Tier1)→일반, L2 주의(Tier2)→주의관찰 플래그, L3 경계→돌봄+위기상담 노출+관리자알림후보, L4 위기→돌봄+위기상담 강조+관리자알림. 근거: docs/research/01 |
| D15 | 데이터 라벨 반입 | **Train/Val 라벨 JSON 확보 완료** | 작품 33,401 / 문장 230,898. 음성(wav) 미반입·불필요. emotion 7종·style 53종(소개의 "60"과 다름). 상세: docs/research/02 |
| D16 | ERD (DB 스키마) | **4-테이블 린 정규화** | work(작품) / sentence(문장) / label(style·emotion 쌍) / sentence_label(N:M). recitation은 sentence의 recitation_src_id+seq로 폴딩(테이블 절약). 음성·per-voter votes 드롭, votes는 agree_ratio로 비정규화. 상세 아래 § 7 |
| D17 | 카드 정령(타입) 결정 기준 | **사용자(아이) 본인의 감정** | 골라준 시구의 감정이 아니라 분석된 아이의 감정으로 정령 결정(슬픈 아이=방울이). 근거: 정서적 타당화 우선(toxic positivity 회피) → 정령=내 감정 비춤 / 한마디·편지=위로. SEL·정서문해력, 숨은 목적(같은 정령 반복=위기신호)과 부합. 구현: route.ts |
| D18 | 문구 선정 로직 개선(①②③) | **시(詩) 전용 풀 + 기능(톤) 매칭 + 기능 LLM 선별** | ① `genre='시'` + 자기완결 길이 12~70자(대사 파편 배제) ② 매칭 톤을 '감정 미러'→'기능'으로: 위로/응원=따뜻·희망(기쁨/무감정), **슬픔 미러링 제거** ③ select 프롬프트 "이 아이를 위로/축하/응원하는, 완결되고 따뜻한 시구를 골라라. 우울·절망·파편 제외". D13 정제(핵심 5단계 구조 불변), D11 벡터는 로드맵 A로 이연. 변경 파일: queries.ts/rubric.ts/select.ts/index.ts/route.ts. 후보 부족 시 전체 장르 폴백 |

### 핵심 파이프라인 (5단계)
1. 이용자 심정 입력
2. 감정 분석 — **긍/부가 아니라 "부정어가 얼마나 많은가"를 척도로** 부정도 측정
3. 감정 라벨과 매칭하여 문구 후보군 추출
4. RAG로 후보군 중 맥락에 가장 맞는 문장 **1개** 선별
5. 문구 강조 디지털 카드 생성 + 그 아래 문구를 활용한 **2문장 편지** 서술

---

## 3. 진행 중 / 검토 중 (논의 필요 — 아직 확정 아님)

| # | 항목 | 현재 기울어진 방향(제안) | 상태 |
|---|------|----------------------|------|
| O6 | 프로젝트 정식 명칭 | 가제 "마음동굴" | 미확정 |

---

## 4. 의도적으로 뒤로 미룬 것 (Deferred — 안 하기로 한 게 아니라 "나중에")

| # | 미룬 항목 | 이유 | 재개 조건 |
|---|----------|------|----------|
| F1 | **로그인 / 인증** | 지금은 익명 이용(이름+연령만). 초기 검증에 불필요 | 회원 관리 기능 확장 시 |
| F2 | **회원 관리** | F1과 연동 | 사용자 누적·재방문 추적이 필요해질 때 |
| F3 | **관리자 모니터링(원래 5번 요구)** | 로그인이 없어 끝까지 개인 추적 불가. 위기 학생 리스트의 전제가 인증 | 인증 도입 후. 그 전까지는 "이름+연령+위기 글" 집계 화면 수준으로 축소 검토 가능 |

> **주의**: F3(관리자 위기 모니터링)은 대표님의 원래 요구사항 5번이다.
> 인증을 미루면서 함께 이연됐음을 반드시 기억할 것. 추후 인증 도입 시 1순위로 복원.

---

## 5. 다음 작업 (Next)

1. ✅ DB 적재 (work/sentence/label/sentence_label) — 진행 중
2. 4단계 RAG용 **문장 임베딩 생성** (임베딩 모델 선정 → sentence.embedding F32_BLOB 채우기 → 벡터 인덱스). 적재와 분리된 후속 단계.
3. **전략서 본문** 작성:
   - DB ERD 문서화 (§7 기반, 정규화 + 반정규화 구분) — 일부 완료
   - UI/UX 전략 & UX 라이팅 (초등생 vs 대학생)
   - 감정 분석 루브릭 (D14) → 시스템 행동 연결
   - 두 축 매칭·분기 출력 설계 (D12·D13)
4. O6 프로젝트 정식 명칭 확정

---

## 6. 변경 이력

| 날짜 | 내용 |
|------|------|
| 2026-06-17 | 최초 작성. D1~D8 확정, F1~F3 이연 결정 |
| 2026-06-17 | O1→D9 확정: 감정 분석 엔진 = 하이브리드 |
| 2026-06-17 | O4→D10 확정: 데이터 미확보, 신청부터 전략서 포함 |
| 2026-06-17 | O3→D11 확정: RAG = Turso 네이티브 벡터 검색 |
| 2026-06-17 | O5→D12 확정: 출력 분기 = 두 축(부정도 게이트 + 의도 결) |
| 2026-06-17 | 숨은 목적 명문화: 학생 정서 추적→학업중단 예방→학적 유지. O2 루브릭을 드롭아웃 조기경보 관점으로 재정의 |
| 2026-06-17 | Next.js 구현(모듈화: 공유 파이프라인+audience 분기). GitHub 푸시. QA/QC: Phase1 22/0·Phase2 19/0 = 41/0. 적재 교훈 §8-1, 진행지점 §8-2 기록. 보고서 docs/qa/QA_REPORT.html |
| 2026-06-17 | **프런트 대개편(표현 계층만, 핵심 로직 D9~D16 불변)**: 초등 수집 카드(마음 카드=포켓몬+인사이드아웃 컨셉, 감정정령 7종, 등급·HP·번호, 원문+문학문구, 다운로드·공유). 홈페이지 쇼케이스(히어로+카드 큐레이션+정령 도감+3단계). 상세 §9 |
| 2026-06-17 | O2 선행연구 완료(docs/research/01). 실제 데이터 반입·프로파일링(docs/research/02): 작품33,401/문장230,898, emotion7·style53 |
| 2026-06-17 | D13 확정: 매칭방향=C(레벨별 분기). D14 확정: 루브릭 5단계. D15 데이터 반입. D16 ERD 4테이블. DB 적재 착수 |
| 2026-06-17 | **문구 선정 개선 ①②③ 적용(D18)**: 시 위주+기능 톤 매칭+기능 LLM 선별. "단추가 없지" 류 대사 파편 제거. **카드 디자인·컨셉 불변(콘텐츠 선정 방식만 변경)**. 정령=아이 감정(D17). 문구 강화 로드맵 §10 |

---

## 7. ERD (D16 상세)

**원칙**: 최대한 정규화하되, *조회 성능에 직결되는 비정규화만* 명시적으로 허용.

```
work (작품 = literature)
  work_id PK (예: S0107)   title   author   translator
  genre (시/소설/희곡/시나리오)   period_raw   period_norm   publish_year

sentence (문장 = 카드 문구의 단위, 핵심)
  id PK   work_id FK→work
  recitation_src_id   seq          -- (비정규화) recitation 테이블 대신 그룹키+순서로 폴딩 → 원문 맥락 재구성용
  origin_text         ptr          -- ptr=운율 끊어읽기(낭독 데이터 특성, 보존)
  vote_total  vote_agree  agree_ratio  -- (비정규화) per-voter votes 원천은 드롭, 집계만. 품질 필터(≥0.8)에 매 조회 사용
  embedding F32_BLOB(dim)            -- 4단계 RAG용. 적재 후 별도 단계에서 채움(현재 nullable/미생성)

label (style·emotion 쌍 = 라벨 어휘)
  id PK   style   emotion   UNIQUE(style, emotion)   -- style이 emotion에 1:1로 안 떨어짐(13개 중복) → 쌍 자체가 라벨

sentence_label (N:M)
  sentence_id FK   label_id FK   PK(sentence_id, label_id)
```

**드롭한 것**: 음성 전 필드(voice/voice_piece/wav), 낭송자(reciter, 성우라 텍스트 서비스 무관), per-voter 투표 원천.
**비정규화 명시**: ① sentence.work_id(genre/period 조인 없이 필터), ② sentence.agree_ratio(품질 필터), ③ recitation을 sentence 컬럼으로 폴딩(테이블 절약).
**조회 경로**: 3단계=sentence_label⋈label에서 emotion 필터 + agree_ratio≥0.8 → 4단계=embedding 벡터 정렬. 메타(장르/시대)=work 조인.

## 8. 운영 메모
- **DB 자격증명**: `maeum-donggul/.env` (TURSO_DATABASE_URL, TURSO_AUTH_TOKEN). `.gitignore`로 커밋 제외. 코드 하드코딩 금지.
- **원천 데이터**: `maeum-donggul/data_raw/extracted/{train,val}/*.json` (gitignore됨, 용량 큼).
- **적재 스크립트**: `maeum-donggul/scripts/` (load.py = 초기, finish_load.py = 마무리/재개).

### 8-1. ⚠️ Turso 대량 적재 교훈 (반드시 숙지 — 재적재 시 시간 좌우)
1. **원격(us-east-2) 왕복 지연이 병목.** libsql 클라이언트는 배치당 1회 네트워크 왕복 → 행 단위 `executemany`는 치명적으로 느림(행마다 왕복). **다중행 INSERT(한 statement에 N행)** 필수.
2. **배치 크기 실측 최적값**: sentence(9컬럼, 긴 한글 텍스트) = **1000행/배치(≈1.58s, 633행/s)**. 1500행은 페이로드 과대로 **정지**(에러 없이 멈춤). sentence_label(2컬럼, 정수) = 4000행 가능. 즉 **행수가 아니라 페이로드 크기가 한계** — 텍스트 컬럼은 배치를 키우지 말 것.
3. **Turso는 외래키(FK)를 강제한다** (일반 SQLite 기본 OFF와 다름!). → **적재 순서 고정**: work → label → **sentence(부모) → sentence_label(자식)**. 자식 먼저 넣으면 `SQLITE_CONSTRAINT: FOREIGN KEY constraint failed`.
4. **멱등 재개**: 모든 INSERT는 `INSERT OR IGNORE`. 중단 후 재개 시 DB에서 기존 id를 `SELECT`해 메모리 목록을 필터링하면 재전송 낭비 제거(finish_load.py 방식).
5. **libsql 파라미터는 tuple로** 전달(list 불가: `'list' object cannot be converted to 'PyTuple'`). 변수 한도 ~32766 내(1000행×9컬럼=9000 OK).
6. **백그라운드 stdout은 버퍼링** → 진행 확인은 별도 연결로 `SELECT count(*)` 직접 조회가 확실.
- 전체 규모: sentence 259,417 / sentence_label 414,048 → 최적 배치로 약 4~5분.

### 8-2. 📌 적재 진행 지점 (2026-06-17, 데모 우선 처리)
- **sentence: 219,000 / 259,417 적재됨.** 나머지 **약 40,417건 미적재(이연)**.
- **sentence_label: 349,351건 적재 완료** (적재된 219k 문장 한정, `scripts/demo_load.py`, 배치 8000, 943s 소요).
- **현재 상태(데모/QA 충분)**: 7개 감정 모두 품질≥0.8 후보 2.6만~3.4만건씩 확보(기쁨30,396/슬픔33,783/분노33,439/불안31,136/당황32,679/상처29,814/무감정26,553).
- **남은 보충 작업(데모 후)**: ① 미적재 sentence ~40k 마저 적재 → ② 그 문장들의 sentence_label 추가. `scripts/finish_load.py`(문장 먼저→라벨, 멱등)로 재개. 반드시 FK 순서 준수.
- 재개 명령: `python3 scripts/finish_load.py` (기존 id는 자동 스킵).

---

## 9. 프런트엔드 / 표현 계층 (2026-06-17 대개편)

> ⚠️ **거버넌스: 이 절의 모든 변경은 "표현(프런트)"이며, 핵심 로직(D9~D16: 감정분석·루브릭·검색·선별·편지)은 손대지 않았다.** 카드는 파이프라인 *출력 위에* 얹는 별도 모듈이다. 로직 변경이 필요해지면 반드시 §2 결정표를 먼저 갱신할 것.

### 9-1. 모듈 경계 (무엇이 코어이고 무엇이 표현인가)
- **코어 로직(불변)**: `lib/pipeline/*`, `lib/audience/*`(페르소나), `lib/db/*`, `lib/llm/*`, `lib/lexicon/*`, `lib/types.ts`. → 5단계 파이프라인의 의미.
- **표현 계층(이번 추가/변경)**:
  - `lib/card/spirit.ts` — 감정→정령·색·등급·HP·번호(순수 표현 메타). 파이프라인과 무관.
  - `lib/card/samples.ts` — 홈 큐레이션용 전시 카드 데이터(실데이터 아님).
  - `components/Spirit.tsx` — 감정 정령 SVG(7종, 인사이드아웃식 글로우).
  - `components/MaeumCard.tsx` — 수집 카드 + 다운로드/공유(html-to-image, Web Share).
  - `components/CaveExperience.tsx` — 초등=MaeumCard, 그 외=기본 카드로 분기(공통 입력/호출 로직 공유).
  - `components/landing/*` — CardGallery, SpiritDex.
  - `app/page.tsx` — 홈 쇼케이스. `app/globals.css` — 스타일.
- **API 연결(가산만)**: `app/api/respond/route.ts`는 파이프라인 결과에서 `cardMeta`를 **파생**해 응답에 추가했을 뿐, 파이프라인 호출/로직은 그대로.

### 9-2. 마음 카드(수집 카드) 컨셉 — 기획 보강
- 레퍼런스: **포켓몬 카드(수집 욕구)** + **인사이드 아웃(감정 캐릭터)** + **인스타그램(예쁘게→자랑)**.
- 감정 7종 = "타입": 기쁨☀반짝이·슬픔💧방울이·분노🔥이글이·불안🌫안개·당황⚡번쩍이·상처🩹보들이·무감정🌙구름이.
- 카드 구성: 정령(글로우 SVG) + 마음 HP + 등급(노멀/레어/★홀로 포일) + No.NN/60 + **내가 쓴 원문(오늘 내 마음)** + 문학 한마디(+출처) + 다운로드(배경화면)·공유(친구 자랑).
- 등급/번호/HP는 `buildCardMeta`로 산출: 같은 스타일=같은 번호·정령(수집 일관성), 등급은 문장 id 시드(뽑을 때마다 설렘, 노멀70·레어22·홀로8).

### 9-3. 홈페이지 (실서비스 쇼케이스)
- 컨셉: "AI 이모션 스페이스 · 동굴". 스크롤형 1페이지.
- 구성: 히어로(가치제안+CTA+감정 오브) → **카드 큐레이션 갤러리**(자랑하고 싶은 카드 피드) → 감정 정령 7종 도감(수집 욕구) → 3단계 이용법 → 두 동굴 입구.
- 초등 UX 라이팅: 쉽고 다정·설렘 위주("뭐든 적어요", "정령이 너를 찾아올까", "자랑해요"). 청년 입구는 별도 담백 톤 유지.

### 9-4. 의존성 추가
- `html-to-image`(카드 이미지 다운로드/공유), `playwright`(QA 캡처, devDep).

### 9-5. 남은 표현 과제(이연)
- 청년(adult) 카드는 아직 기본 카드 — 추후 청년 톤의 카드 디자인 별도.
- "내 마음 갤러리(도감 수집함)" = 모은 카드 저장/그리드 → 로그인·회원관리 도입 후(F1·F2와 연동).

---

## 10. 문구 선정 강화 로드맵 (D18 후속)

> 현재(1차): 시(詩) 전용 풀 + 기능 톤 매칭 + 기능 LLM 선별. "단추가 없지" 류는 사라졌으나 일부 시구는 *느슨하게* 맞음(편지가 보정). 정밀도를 더 올리는 단계:

| 단계 | 내용 | 효과 / 비용 | 우선순위 |
|---|---|---|---|
| **B. 기능 큐레이션** | 시구 풀(약 47,111개 자기완결 시구)에 LLM 오프라인 배치로 **위로/축하/응원 적합도 + 명구성 + 초등 적합도** 점수를 미리 매겨 컬럼/태그로 저장 → 검색이 "검증된 풀"에서만 추출 | "확실히 위로/축하" 실현, 로직이 탄탄. Haiku 배치라 저렴 | **다음 작업 권고** |
| **A. 벡터 임베딩** | 시구·사용자 글을 의미 벡터로 임베딩 → 감정뿐 아니라 *의미*로 매칭(이연했던 D11 4단계 본구현). sentence.embedding F32_BLOB + libSQL 벡터 인덱스 | 정밀도 최고. 임베딩 1회 생성 필요 | 그다음 |
| **D. 데이터 보강** | ① 더러운 값 정리(author "nan", period junk 등) ② **공개 명시(名詩) 추가** — 김소월·윤동주·정지용 등 공유저작물 한국 근대시(아이에게 익숙·따뜻) ③ 초등 난이도 필터 | 인지도·품질 급상승, "데이터 강화" 직결 | B와 병행 |

**권고 순서**: B(기능 큐레이션) → D②(명시 추가) 병행 → A(임베딩)로 정밀도 마무리.
